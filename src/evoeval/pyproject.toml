[project]
name = "evoeval-adapter"
version = "0.1.0"
description = "EvoEval adapter for Harbor"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["harbor==0.23.0"]

[build-system]
requires = ["hatchling>=1,<2"]
build-backend = "hatchling.build"

[tool.uv]
package = false
